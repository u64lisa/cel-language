package "test";

var start = epoch();

println("starting...");

function fib(x: long) -> long {
    #optimize;
    if (x <= 1) return x;

    return fib(x - 1) + fib(x - 2);
}
println(fib(47));

var time = |float| (epoch() - start);
println(str(time) + " ms");